<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.echat.arkarcy.laundry" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".addcustomerform"
                  android:label="Add Customer Information"/>
        <activity android:name=".customerDetails"
                  android:label="Customer Details">

        </activity>
        <activity android:name=".Clothes"
                  android:label="Cloths Detail">
        </activity>
        <activity android:name=".showRecord" >
        </activity>
        <activity android:name=".incomeinfo" android:label="Income">
        </activity>
    </application>

</manifest>